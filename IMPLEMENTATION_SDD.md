# Implementation SDD

This repository now contains a maintainer-requested SDD package for an interactive diffusion prototype.

The existing repository remains the UDAM theory repository. The implementation prototype is specified separately under:

```text
docs/sdd/
```

Start here:

1. `AGENTS.sdd.md`
2. `docs/sdd/README.md`
3. `docs/sdd/spec.md`
4. `docs/sdd/tasks.md`
5. `docs/sdd/acceptance.md`

The v0.1 prototype target is:

```text
Prompt + Human Layer + Noise Brush + Snapshot-based exploration of intermediate diffusion states.
```

Do not implement beyond `docs/sdd/spec.md` without updating the SDD documents first.
